"""
{title}

{motivation}

Hypothesis: {hypothesis_statement}

Generated by SciWeave from: {source}
"""

from typing import Dict, Any

from sciweave import Project, Experiment


class {class_name}(Experiment):
    """
    {title}

    Config parameters (tracked in MLflow):
{config_docstring}

    Result metrics (tracked in MLflow):
{results_docstring}
    """

    def run(self) -> Dict[str, Any]:
        """
        Execute the experiment and return results.

        The config is accessible via self.config (flattened for MLflow params).

        Returns:
            Dict containing all result metrics to track.
        """
        config = self.config

        # TODO: Implement experiment logic here
        # Access config values like: config["model_name"], config["learning_rate"], etc.
        #
        # Implementation notes:
        # {implementation_notes}

        # Return results dictionary
        # All keys will be automatically logged as MLflow metrics
        return {{
{results_return}
        }}


# ============================================================================
# Boilerplate for running the experiment
# ============================================================================

def create_config() -> dict:
    """Create default config for this experiment."""
    return {{
{config_defaults}
    }}


def main():
    """Run the experiment with default config."""
    # Initialize project
    project = Project("experiments", tracking_uri="databricks")

    # Create config (modify as needed)
    config = create_config()

    # Create and run experiment
    exp = {class_name}(
        project=project,
        experiment_name="{experiment_name}",
        config=config,
    )

    # Execute experiment (calls run() internally)
    results = exp()

    print("Results:", results)
    return results


if __name__ == "__main__":
    main()
